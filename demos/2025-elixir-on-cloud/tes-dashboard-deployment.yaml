apiVersion: v1
kind: Service
metadata:
  name: tes-dashboard-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: tes-dashboard
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: http
